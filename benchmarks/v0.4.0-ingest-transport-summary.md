# v0.4.0 Ingest Transport Summary

Reference run date: **April 6, 2026**

Reference machine:

- `uname -a`: `Darwin macbook 25.4.0 ... arm64`
- CPU: `Apple M4 Pro`
- Toolchain: `nix develop -c zig version` -> `0.15.1`

These numbers are a checked-in preview-readiness baseline, not a performance promise. The main goal is to keep the local-ingest story honest relative to the real live HTTP path and to make transport-bound vs storage-bound regressions obvious.

## Commands

```sh
nix develop -c zig build bench-ingest-transport -- --scenario one_hot_one_writer
nix develop -c zig build bench-ingest-transport -- --scenario fanout_four_writers
nix develop -c zig build bench-ingest-transport -- --scenario warm_declared_10k
nix develop -c zig build bench-ingest-transport -- --scenario cold_declare_10k
nix develop -c zig build bench-ingest-transport -- --scenario steady_state_100k
```

Benchmark notes:

- The HTTP lane now uses the real `sydradb serve` process and `POST /api/v1/ingest`.
- The live HTTP benchmark client now sends and handles `Expect: 100-continue` correctly instead of suppressing it.
- Benchmark lines now split `active_ms` from `barrier_ms` and include declaration-phase totals (`exact_decl_metric_s`, `exact_decl_series_s`, `exact_decl_wal_s`, `exact_decl_tag_s`) plus declaration outcome counters.
- The direct-engine cold scenario now includes declaration work inside the timed window.

## Results

| Scenario | Class | Transport | Throughput points/s | Elapsed ms | Active ms | Barrier ms | Key note |
| --- | --- | --- | ---: | ---: | ---: | ---: | --- |
| `one_hot_one_writer` | transport-bound | `direct_engine` | 624181 | 16.02 | 0.13 | 15.89 | direct baseline still mostly barrier/flush |
| `one_hot_one_writer` | transport-bound | `cli_direct_ndjson` | 96301 | 103.84 | 103.45 | 0.40 | parser/apply path dominates |
| `one_hot_one_writer` | transport-bound | `http_ndjson` | 45832 | 218.19 | 217.85 | 0.34 | live HTTP upload dominates |
| `one_hot_one_writer` | transport-bound | `uds_binary_warm` | 708366 | 14.12 | 1.97 | 12.15 | warm socket path beats direct in this sample |
| `fanout_four_writers` | transport-bound | `direct_engine` | 387222 | 25.83 | 0.09 | 25.73 | direct baseline still barrier-heavy |
| `fanout_four_writers` | transport-bound | `cli_direct_ndjson` | 143371 | 69.75 | 41.23 | 28.52 | parser/apply still visible |
| `fanout_four_writers` | transport-bound | `http_ndjson` | 53060 | 188.47 | 163.55 | 24.92 | concurrent live HTTP now stable |
| `fanout_four_writers` | transport-bound | `uds_binary_warm` | 461255 | 21.68 | 0.75 | 20.93 | warm UDS stays clearly ahead of live HTTP |
| `warm_declared_10k` | storage-bound | `direct_engine` | 15259 | 655.33 | 0.14 | 655.20 | almost all time is flush/barrier |
| `warm_declared_10k` | storage-bound | `cli_direct_ndjson` | 13508 | 740.30 | 240.00 | 500.30 | declaration already outside timed window |
| `warm_declared_10k` | storage-bound | `http_ndjson` | 15441 | 647.63 | 440.05 | 207.58 | live HTTP remains reference-only here |
| `warm_declared_10k` | storage-bound | `uds_binary_warm` | 14018 | 713.35 | 2.12 | 711.24 | passes storage-bound gate, but remains barrier-heavy |
| `cold_declare_10k` | storage-bound | `direct_engine` | 13226 | 756.08 | 173.38 | 582.70 | declaration now timed in direct baseline |
| `cold_declare_10k` | storage-bound | `cli_direct_ndjson` | 14235 | 702.52 | 208.69 | 493.83 | parser path stays competitive here |
| `cold_declare_10k` | storage-bound | `http_ndjson` | 12941 | 772.72 | 436.63 | 336.09 | HTTP remains reference-only for this class |
| `cold_declare_10k` | storage-bound | `uds_binary_cold` | 10821 | 924.15 | 156.50 | 767.65 | declaration burst is no longer the only issue; barrier/flush still dominates total elapsed time |
| `steady_state_100k` | transport-bound | `direct_engine` | 1690017 | 59.17 | 1.07 | 58.10 | sustained baseline |
| `steady_state_100k` | transport-bound | `cli_direct_ndjson` | 97101 | 1029.86 | 1029.14 | 0.72 | parser/apply path dominates |
| `steady_state_100k` | transport-bound | `http_ndjson` | 45018 | 2221.35 | 2220.97 | 0.38 | large live HTTP upload |
| `steady_state_100k` | transport-bound | `uds_binary_warm` | 1736202 | 57.60 | 17.94 | 39.66 | sustained warm UDS edges out direct in this sample |

## Gate Readout

Promotion gates are now split by workload class.

Transport-bound scenarios:

- warm UDS must stay within 25% of direct-engine throughput
- warm UDS must beat live HTTP by at least `1.5x`

Observed:

- `one_hot_one_writer`: pass on both gates
  - UDS is about `1.135x` direct engine
  - UDS is about `15.46x` faster than live HTTP
- `fanout_four_writers`: pass on both gates
  - UDS is about `1.191x` direct engine
  - UDS is about `8.69x` faster than live HTTP
- `steady_state_100k`: pass on both gates
  - UDS is about `1.027x` direct engine
  - UDS is about `38.57x` faster than live HTTP

Storage-bound scenarios:

- warm/cold UDS must stay within 10% of direct-engine throughput
- live HTTP stays a reference number and is not a promotion blocker

Observed:

- `warm_declared_10k`: pass
  - UDS is about `0.919x` direct engine
  - declaration remains visible in metrics (`local_declare_s=0.134607`) but the measured bottleneck is still the barrier/flush path
- `cold_declare_10k`: fail
  - UDS is about `0.818x` direct engine
  - declaration cost dropped materially (`local_declare_s=0.135337`, `exact_decl_tag_s=0.020046`), but the storage-bound barrier path still keeps total elapsed time above the preview bar

## Conclusion

The local socket path is now clearly the right same-host exact-series transport in transport-bound workloads, and the benchmark output itself shows why: warm UDS stays at or above direct-engine throughput in the current samples while live HTTP remains far slower.

The remaining promotion blocker is no longer `warm_declared_10k`. That case now behaves like a true storage-bound workload and UDS is already at direct-engine parity there.

The remaining blocker is `cold_declare_10k`: declaration-heavy cold starts improved materially in this cycle, but the preview gate still fails because the same workload remains slower end-to-end over the socket path than the direct-engine baseline. The next exact-series performance work should stay focused on cold declaration plus storage-bound barrier cost rather than broadening socket scope.
