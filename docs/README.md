# SydraDB Documentation Site

This directory is a standalone Docusaurus site for the SydraDB project.

## Quick start

```sh
cd docs
npm install
npm run start
```

## Build

```sh
cd docs
npm run build
npm run serve
```

## Legacy docs backup

The previous contents of `/<repo root>/docs` were moved into `docs/_backup/` (timestamped).

